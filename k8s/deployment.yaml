apiVersion: apps/v1
kind: Deployment
metadata:
  name: exercise-service
  labels:
    app: exercise-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exercise-service
  template:
    metadata:
      labels:
        app: exercise-service
    spec:
      imagePullSecrets:
        - name: ecr-secret
      containers:
        - name: exercise-service
          image: 239861161036.dkr.ecr.eu-central-1.amazonaws.com/codesnap/exercise-service:v1
          imagePullPolicy: Always
          ports:
            - containerPort: 8002
          env:
            - name: DATABASE_URL
              value: "postgresql://postgres:postgres@postgres-exercise-service:5432/codesnap"
            - name: NATS_URL
              value: "nats://nats:4222"
            - name: LOGFIRE_TOKEN
              valueFrom:
                secretKeyRef:
                  name: logfire-secrets
                  key: write-token
            - name: LOGFIRE_SERVICE_NAME
              value: "exercise-service"

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
